<%= form_for(@job) do |f| %>
  <% if @job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% @job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :employer_id, "Employer: #{@employer_name}" %><br>
    <%= f.hidden_field :employer_id, value: @employer_id %>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :category_id, "Category" %><br>
    <%= f.select :category_id, options_for_select(@category.map{ |s| [s.name, s.id] }) %>
  </div>
  <div class="field">
    <%= f.label :deadline %><br>
    <%= f.date_select :deadline %>
  </div>
  <div class="field">
    <%= f.label :tags, "Tags: (Enter multiple tags separated by commas)" %><br>
    <%= f.text_area :tags, value: @tags %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
